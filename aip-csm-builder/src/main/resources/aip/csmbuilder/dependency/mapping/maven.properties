# Native Maven <scope> -> CSM DependencyKind mapping.
#
# Starter entries only (tasks.md 10.3) - exhaustive coverage is
# reference data, not an architectural concern, and may be extended
# incrementally without a code change (just add or edit an entry
# here). An unmapped scope string is a legitimate, honest outcome -
# see DependencyKindClassifier - never treated as an error.
#
# 'version' is a reserved key (not a scope mapping) read by
# ResourceDependencyKindClassifier.versionOf(...).

version=1

compile=COMPILE_TIME
provided=COMPILE_TIME
system=COMPILE_TIME
import=COMPILE_TIME
runtime=RUNTIME
test=TEST_ONLY
