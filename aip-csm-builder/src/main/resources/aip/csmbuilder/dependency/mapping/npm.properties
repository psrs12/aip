# Native npm package.json dependency section -> CSM DependencyKind
# mapping.
#
# Starter entries only (tasks.md 10.3) - see maven.properties for the
# extension/versioning convention this file follows.

version=1

dependencies=COMPILE_TIME
devDependencies=TEST_ONLY
peerDependencies=COMPILE_TIME
optionalDependencies=COMPILE_TIME
